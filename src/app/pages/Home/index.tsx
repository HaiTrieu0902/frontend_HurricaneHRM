import React from 'react';
import Layout from '../../layout';

const HomePage = () => {
    return (
        <Layout>
            <div>hihi</div>
        </Layout>
    );
};

export default HomePage;
